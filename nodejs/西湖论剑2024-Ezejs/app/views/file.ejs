<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>文件重命名</title>
    <link rel="stylesheet" href="/styles/bootstrap.min.css">
    <style>
        /* 自定义样式规则，增大文件上传标签的字体大小 */
        .custom-label {
            font-size: 20px; /* 根据需要调整字体大小 */
        }
        .custom-label2 {
            font-size: 50px; /* 根据需要调整字体大小 */
        }
        /* 添加渐变背景 */
        body {
            background: linear-gradient(to bottom, #3498db, #e52d2d); /* 渐变色范围和颜色可以根据需要调整 */
            height: 100vh; /* 使body占满整个视窗，确保背景图片完全显示 */
            margin: 0; /* 清除body的默认外边距 */
            display: flex;
            align-items: center;
            justify-content: center;
        }
    </style>
</head>
<body>


<center>
    <label for="formFile" class="form-label mt-4 custom-label2">文件读取</label>
    <form action="/file" method="get">
        <div class="form-group">
            <label class="col-form-label mt-4 custom-label" for="inputDefault">请输入uuid</label>
            <input type="text" class="form-control" placeholder="uniqueFileName" id="uniqueFileName" name="uniqueFileName">
        </div>
        <br>
        <button type="submit" class="btn btn-info" >查看文件</button>
    </form>
</center>
</body>
</html>